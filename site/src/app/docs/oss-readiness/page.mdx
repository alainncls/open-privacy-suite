import { Callout } from "@/components/mdx/callout";

export const metadata = {
  title: "OSS Readiness Checklist",
  description:
    "Current source-only Apache-2.0 open-source readiness checklist for Privacy Proxy.",
};

# OSS Readiness Checklist

Snapshot: July 7, 2026. Scope: publishing the Privacy Proxy source repository
under Apache-2.0. This checklist intentionally separates source publication
from official binary and container image releases.

<Callout variant="warning" title="Current verdict">
  Source-only Apache-2.0 publication is close, but the coordinated Gateway
  repositories and public workflow posture still need final handling. Do not
  publish official binaries or Docker images until SBOM, provenance, notices,
  and LGPL binary-distribution obligations are complete.
</Callout>

## Done On The OSS Readiness Branch

| Status | Item |
|--------|------|
| Done | Added root Apache-2.0 `LICENSE`. |
| Done | Changed the README license section from MIT to Apache-2.0. |
| Done | Added `license: Apache-2.0` to npm package manifests and checked-in lockfile root metadata. |
| Done | Added starter `NOTICE`. |
| Done | Added starter `THIRD_PARTY_NOTICES.md`. |
| Done | Added `SECURITY.md`, `CONTRIBUTING.md`, `CODE_OF_CONDUCT.md`, and `GOVERNANCE.md`. |
| Done | Ignored internal audit report filenames so they are not accidentally committed. |
| Done | Switched `tools/wallet-emulator-js` submodule URL from SSH to public HTTPS. |
| Done | Aligned project-owned Solidity SPDX identifiers with Apache-2.0. |
| Done | Removed the accidental `.agents` gitlink on the base hygiene branch. |

## Must Finish Before Making The Source Repo Public

| Priority | Item | Why it matters |
|----------|------|----------------|
| Critical | Confirm legal copyright holder text for `NOTICE`. | The public notice needs the correct legal owner before launch. |
| Critical | Confirm the private vulnerability contact in `SECURITY.md`. | The public repo needs a working private disclosure path. |
| Critical | Publish or coordinate `gateway-fm/chain-indexer` under Apache-2.0. | Privacy Proxy references chain-indexer proto/generated code and privacy-mode deployment depends on it. |
| Critical | Publish or coordinate `gateway-fm/block-explorer` under Apache-2.0. | Privacy-mode docs and compose workflows depend on this sibling repo. |
| Critical | Decide whether `gateway-fm/wallet-emulator-js` is public Apache-2.0; otherwise remove or replace the submodule. | A public source repo should not point at a private or unclearly licensed submodule. |
| High | Make `.github/workflows/privacy-bypass.yml` safe for public forks without `CROSS_REPO_READ_TOKEN`, or disable it for public PRs until sibling repos are public. | Public contributors should not hit broken/private cross-repo checkout behavior. |
| High | Scrub public docs for private Linear URLs and internal roadmap wording. | Public docs should not expose internal planning systems or confusing private context. |
| Medium | Decide whether to keep `RD-XXXX` references outside migrations or strip them in a mechanical follow-up. | These references are not secrets, but they are noisy internal tracking artifacts. |
| Medium | Decide whether to keep `.mcp.json` tracked publicly or move it to an example file. | The file is not secret, but it auto-configures a local MCP command. |
| Medium | Decide git-history email posture: accept as normal OSS attribution or rewrite before public launch. | Git history contains contributor attribution; decide before the repo becomes public. |
| Medium | Confirm inbound contribution policy: DCO is documented; add CI enforcement if required. | The policy is documented, but enforcement is a maintainer decision. |

## Prompts To Run Next

Use these prompts as task briefs. Each prompt should produce either a focused PR or a written decision record.

### Legal notice, security contact, and inbound policy

```text
Take the current OSS readiness branch for gateway-fm/privacy-proxy. Verify the public source-only Apache-2.0 legal artifacts: LICENSE, NOTICE, SECURITY.md, CONTRIBUTING.md, CODE_OF_CONDUCT.md, GOVERNANCE.md, README license text, and package metadata. Confirm the correct copyright holder, security contact, and inbound contribution posture. Make only minimal public-source-readiness edits and list any remaining legal decisions that require counsel.
```

### Gateway sibling repositories

```text
Audit gateway-fm/chain-indexer and gateway-fm/block-explorer for source-only Apache-2.0 publication readiness. Check LICENSE, NOTICE, README license text, package metadata, third-party notices, generated code provenance, secrets, private links, CI behavior for public forks, and image/binary release blockers. Produce a short blocker list and minimal PR-ready fixes.
```

### Wallet emulator submodule decision

```text
Determine whether gateway-fm/wallet-emulator-js can be made public under an Apache-compatible license before privacy-proxy is public. If yes, prepare the minimum license/notice/metadata changes in that repo. If no, prepare a privacy-proxy PR that removes or replaces the submodule and updates docs/tests that refer to it.
```

### Public-fork CI hardening

```text
Review .github/workflows/privacy-bypass.yml and any cross-repo CI assumptions for public-fork safety. Make the workflow skip or clearly gate private sibling-repo checkouts when CROSS_REPO_READ_TOKEN is unavailable, while preserving manual/internal coverage. Keep changes minimal and document how maintainers should run the full privacy-mode test.
```

### Public docs and internal-reference scrub

```text
Scan README.md, docs/, site/, .github/, scripts/, and tracked config for private Linear URLs, internal roadmap language, and unnecessary RD-XXXX references. Do not remove useful technical context. Propose or make low-risk wording changes that make the repo public-friendly, and leave a separate list of references that should stay because they are migrations, tests, or useful historical identifiers.
```

### Binary and container release licensing

```text
Prepare the pre-release compliance plan for official binaries and Docker images. Generate or specify SBOMs for Go, npm, Solidity, submodules, and container base images. Identify LGPL/GPL/MPL/CC obligations, especially go-ethereum and native rapidsnark, and update THIRD_PARTY_NOTICES.md with scanner-backed data. Do not publish images or binaries as part of this task.
```

## Gateway Repositories To Add To The Apache Plan

| Repository | Source posture |
|------------|----------------|
| `gateway-fm/privacy-proxy` | Publish source under Apache-2.0 after the source-public checklist is complete. |
| `gateway-fm/chain-indexer` | Publish or coordinate under Apache-2.0 before public Privacy Proxy source launch. |
| `gateway-fm/block-explorer` | Publish or coordinate under Apache-2.0 before public Privacy Proxy source launch. |
| `gateway-fm/wallet-emulator-js` | Publish under an Apache-compatible license if retained as a submodule; otherwise remove or replace. |

Each repository and any related image should carry its own `LICENSE`, `NOTICE`,
third-party notices, SBOM, and source-to-image mapping.

## Dependency And License Notes

| Area | Current note |
|------|--------------|
| Apache source with LGPL dependencies | Acceptable for source publication with notice; binary and image distribution need a compliance plan. |
| `github.com/ethereum/go-ethereum` | Main notable LGPL-3.0 dependency for source users. Upstream command binaries are GPL-3.0; this repo uses library packages. |
| `github.com/iden3/go-rapidsnark/...` | Go wrapper modules are understood to be Apache-2.0/MIT. Native `iden3/rapidsnark` remains an LGPL-3.0 binary/image review item. |
| npm dependencies | Checked lockfiles are mostly MIT, ISC, BSD-family, or Apache-2.0, with some MPL/CC/other notice items captured in `THIRD_PARTY_NOTICES.md`. |

## Before First Official Binary Or Image Release

| Status | Item |
|--------|------|
| Open | Generate complete SBOMs for source, binaries, and container images. |
| Open | Run Go, npm, Solidity, and container license scans and update `THIRD_PARTY_NOTICES.md`. |
| Open | Resolve go-ethereum LGPL-3.0 binary/container distribution obligations. |
| Open | Verify whether iden3 rapidsnark LGPL native code is linked, bundled, or dynamically loaded in release binaries. |
| Open | Publish source-to-image mapping for each image tag. |
| Open | Sign images/artifacts and publish checksums/provenance. |

## Explicitly Deferred For Minimal Source-Only Launch

| Status | Item |
|--------|------|
| Deferred | Go module path rename from `privacy-proxy` to a public import path. |
| Deferred | Per-file SPDX headers across Go, TypeScript, and TSX. |
| Deferred | Removing unused dependencies such as indirect Sentry if still present after `go mod tidy`. |
| Deferred | Production Docker/compose hardening beyond obvious public-source blockers. |

## Source Files

- [OSS source-only TODO](https://github.com/gateway-fm/privacy-proxy/blob/oss/source-only-apache2-readiness/OSS_SOURCE_ONLY_TODO.md)
- [Third-party notices starter](https://github.com/gateway-fm/privacy-proxy/blob/oss/source-only-apache2-readiness/THIRD_PARTY_NOTICES.md)
- [NOTICE](https://github.com/gateway-fm/privacy-proxy/blob/oss/source-only-apache2-readiness/NOTICE)
- [SECURITY.md](https://github.com/gateway-fm/privacy-proxy/blob/oss/source-only-apache2-readiness/SECURITY.md)
